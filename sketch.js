function setup() {
  createCanvas(400, 300);
    background(0);
}

function draw() {
  noStroke();
  fill(400, 300, 100)

  circle(mouseX, mouseY, 24);
  
}
function mousePressed(){
  background(0);
}